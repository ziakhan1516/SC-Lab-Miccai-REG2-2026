# ==========================================================================
#  Training / evaluation environment (the "manga" conda env used to train
#  the WSI reasoning model and to run the held-out test evaluation).
#
#  torch/torchvision are installed separately from the CUDA 12.8 wheel index:
#    pip install torch==2.8.0 torchvision==0.23.0 \
#        --index-url https://download.pytorch.org/whl/cu128
#
#  These pinned versions are required for the trained LoRA adapter to load.
# ==========================================================================

transformers==4.57.6
peft==0.17.1
accelerate==1.10.1
safetensors==0.6.2
tokenizers==0.22.2
huggingface-hub==0.35.1
sentencepiece==0.2.1
einops==0.8.2

# CONCH ViT-B/16 feature extraction
timm==1.0.20
ftfy
regex

# data / metrics / utilities
numpy==2.0.2
pandas==2.2.3
scipy==1.15.2
scikit-learn
h5py==3.13.0
Pillow
tqdm==4.67.1
matplotlib==3.10.3
